#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../quoted-printable/bin/quoted-printable" "$@"
  ret=$?
else 
  node  "$basedir/../quoted-printable/bin/quoted-printable" "$@"
  ret=$?
fi
exit $ret
